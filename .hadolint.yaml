ignored:
  # "Pin versions in apt-get install" — we pin via base image, not individual packages
  - DL3008
  # "Pin versions in pip install" — we use uv with lockfile
  - DL3013
  # "Use COPY instead of ADD" — we don't use ADD
  - DL3020
